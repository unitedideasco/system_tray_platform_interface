name: system_tray_platform_interface
description: The flutter package that allows user to create system tray icons on MacOS, Windows and Linux
repository: https://github.com/unitedideasco/system_tray_platform_interface
issue_tracker: https://github.com/unitedideasco/system_tray_platform_interface/issues?q=is%3Aissue+is%3Aopen+label%3A%22p%3A+system_tray_platform_interface%22

# NOTE: We strongly prefer non-breaking changes, even at the expense of a
# less-clean API. See https://flutter.dev/go/platform-interface-breaking-changes
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  pedantic: ^1.10.0